apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ops-pushgateway
  namespace: monitoring
  labels:
    tier: ops
spec:
  selector:
    matchLabels:
      app: ops-pushgateway
  namespaceSelector:
    any: true
  endpoints:
    - port: metric
      interval: 30s
